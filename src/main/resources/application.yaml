spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coding_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update #automatycznie tworzy i updatuje tabele sql na pdst kodu
    show-sql: true # pokazuje jakie query sa wykonywane podczas dzialania programu

